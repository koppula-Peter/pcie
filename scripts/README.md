# scripts/

Build & automation entry points (delivered progressively; see VIVADO_BUILD_GUIDE):

- `create_project.tcl` · `build_synth.tcl` · `build_impl.tcl` ·
  `build_bitstream.tcl` · `run_reports.tcl` · `run_sim.tcl` (M14/M15)
- `gen_regs.py`, `check_traceability.py`, `gen_rdc_matrix.py` (M3+)
- Environment helper sourcing Vivado settings64.sh.
