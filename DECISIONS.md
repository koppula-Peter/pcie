# DECISIONS

Architectural/process decisions log. One entry per decision; superseding a
decision requires a new entry referencing the old ID.

| ID | Date | Decision | Rationale | Alternatives rejected |
|---|---|---|---|---|
| D-001 | 2026-08-24 | Repository root = `/home/peter/Desktop/IP_dev/pcie` (the working directory), i.e., the `pcie_gen5` tree of master §62 maps onto repo root. | Directory already dedicated to this project | Creating nested `pcie_gen5/` subdir |
| D-002 | 2026-08-24 | License: Apache-2.0 proposed; final selection pending owner confirmation. Note: PCI-SIG spec text is never redistributed regardless of code license. | Common for reusable IP; permissive with patent grant | MIT (no patent grant), proprietary |
| D-003 | 2026-08-24 | DMA strategy on CPM5: **deferred to M8** between (a) wrapping hardened QDMA behind our dma_abstraction and (b) custom queue DMA over block AXI4-Stream TL interfaces. Descriptor format spec is ours in both cases. | QDMA gives proven Gen5x8 performance + SR-IOV; custom DMA gives control/isolation. Needs HW-V-04 capability extraction before commitment. | Committing now without data |
| D-004 | 2026-08-24 | Verification stack: xsim+UVM1.2 primary protocol verification; Verilator for fast unit/CI smoke; Icarus only for tiny utilities; cocotb optional CI layer; independent VIP at signoff (B-004 interim plan documented). | Matches installed tools; UVM ships free with Vivado | Commercial simulator dependency now |
| D-005 | 2026-08-24 | **Gen5 x8 production path = CPM5** (TARGET C). PL PCIe5 (TARGET B) is capped at Gen5 x4 per PG343 — used for wide low-gen configs and Gen5x4. | Grounded in PG343 feature table | Attempting Gen5x8 on PL block (impossible) |
| D-006 | 2026-08-24 | Git initialized but **no commits until owner explicitly requests** (project rule compliance). .gitignore pre-seeded. | Version-control hygiene vs commit-permission rule | Immediate initial commit |
| D-007 | 2026-08-24 | All numeric PCIe constants parameterized behind `SPEC_VERIFICATION_REQUIRED` flags until B-001 resolves; RTL may be structured/tested meanwhile. | Rule 0.1 compliance; enables parallel progress | Freezing numbers from memory (forbidden) |
| D-008 | 2026-08-24 | Configuration space registers are single-sourced in YAML (`rtl/cfg/regs/*.yaml`); `scripts/gen_regs.py` generates the SV package, the CSR fabric module, register reference docs, and the Linux uapi header. Hand-written RTL never duplicates generated constants. | PRD-100 single-source-of-truth; eliminates doc/RTL/header drift | Hand-maintained parallel artifacts |
| D-009 | 2026-08-24 | Formal/elaboration toolchain: YoWASP-Yosys 0.68 (pip-installable, reproducible) for elaboration checks and bounded formal; Verilator 5.032 for lint+sim. Resolves OI-003. | No local yosys install; wasmtime wheel is deterministic and CI-friendly | apt install (needs sudo), commercial formal now |
| D-010 | 2026-08-24 | Portable RTL style constraints adopted (see reports/lint_waivers.md): no scoped enum refs in functions, no enum-typed vars/typedefs where yosys elaboration applies, assignment-style function bodies, package-before-import compile order. | Cross-tool (Verilator/Yosys/xsim) compatibility proven by M3 unit tests | Tool-conditional source variants |
| D-011 | 2026-08-24 | CDC primitives (`pcie_sync_2ff`, `pcie_pulse_handshake`) carry only generic `async_reg` attribute; Vivado ASYNC_REG placement enforcement goes to XDC constraints, keeping rtl/ vendor-neutral. | Contamination rule §43 | Embedding ASYNC_REG directly |
| D-012 | 2026-08-25 | **Target pivot:** first production hardware track = Zynq-7000 Gen2 (PG054 adapter; ZC702 = protocol-dev only since Z-7020 has no GTPs/PCIe block per DS190; hardware validation on ZC706-class). Versal CPM5 Gen5 track DEFERRED, not deleted: portable layers unchanged, `vendor/amd/{pl_pcie5,cpm5}/` preserved. Profile naming per ARCH-001 §C supersedes PRD §2 table for new work. | Owner mandate 2026-08-25; DS190 feature-table evidence | Continuing Versal-first against the new mandate |
| D-013 | 2026-08-25 | Standing process rule: git commit+push at every verified milestone; regression reports archived under `reports/<date>_<sha>/`. | 2026-08-25 repo-lineage incident (pcie tree restored from divergent-backup); push-per-milestone mitigation | Local-only milestones |
| D-014 | 2026-08-25 | **Standalone repository:** project extracted from the IP_dev monorepo to its own git repo (`koppula-Peter/pcie`, private). Fresh history rooted at this import; full provenance preserved in commit message + docs. IP_dev retains no pcie content. Owner instruction 2026-08-25 ("move out of ip_dev ... prevent any data loss"). | Decouples PCIe release lifecycle from unrelated monorepo churn; independent remote = independent backup; eliminates nested-repo ambiguity | Staying as monorepo subdirectory |
